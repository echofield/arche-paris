# RLS audit 2025-02-23 — Applied migrations

Migrations added from the RLS inventory (AUDIT 2025-02-23). Apply via `supabase db push` or SQL Editor.

---

## Files

| Migration | Purpose |
|-----------|---------|
| `20260224000001_audit_rls_service_managed.sql` | Enable RLS on `arche_rate_limits` (no policies; service_role only). |
| `20260224000003_audit_rls_service_managed_extra.sql` | Enable RLS on `activation_codes`, `vaults`, `journal_entries`, `rate_limits` (no policies; service_role only). Apply in a separate commit if you prefer atomic PRs. |
| `20260224000002_audit_rls_user_ownership.sql` | Full owner RLS (SELECT/INSERT/UPDATE/DELETE) + GRANTs + indexes for user_id tables: user_zone_state, ritual_runs, engravings, zone_engravings, user_complexion, complexion_deltas, paths, challenges, challenge_attempts, personal_bests, zone_resonance, zone_custodians. |

---

## Optional (if you prefer one migration for all service-managed)

- The four tables in `20260224000003` can be applied together with `20260224000001` by running both; or keep 000003 for a separate commit (“one logical change per PR”).
- **Card-owned tables** (cards, traces, inscriptions, etc.) — Remain service-managed; no client policies. Confirmed by design.
- **Realtime / Storage** — Add room_members or user-uploads policies only if you use those features.

---

## After applying

1. As authenticated user A: SELECT/INSERT/UPDATE/DELETE only on rows with `user_id = A`.
2. As user B: no access to A’s rows.
3. Service-managed tables: client queries blocked; Edge Functions (service_role) succeed.
4. Indexes on `user_id` (and policy columns) present.

**Smoke test:**
- **Read-only (SQL Editor sans écriture):** `docs/RLS_SMOKE_TEST_READONLY.sql` — vérifie RLS, nombre de policies, GRANTs, et comptage service-managed en `authenticated` sans JWT. Aucun INSERT/DELETE/TEMP TABLE.
- **Complet (session avec écriture):** `docs/RLS_SMOKE_TEST.sql` — en plus : insertion de 2 lignes de test, puis nettoyage. Pour “User A / User B”, valider manuellement avec deux comptes réels.

- **GRANTs trop permissifs :** si le smoke test montre anon/authenticated avec privilèges sur tables service-managed ou anon sur user_zone_state, exécuter `docs/RLS_GRANTS_FIX.sql`, puis relancer le smoke test read-only.
- **CI :** `docs/RLS_CI_CHECK.sql` vérifie en lecture seule la présence de RLS et des 4 policies (SELECT/INSERT/UPDATE/DELETE) sur `user_zone_state`. Exécuter avec `psql -v ON_ERROR_STOP=1 -f docs/RLS_CI_CHECK.sql` (ou équivalent) pour faire échouer le pipeline si la config RLS est incomplète.

Commit messages: `AUDIT 2025-02-23: enable RLS on service-managed tables (arche_rate_limits)`, `AUDIT 2025-02-23: enable RLS on service-managed tables (activation_codes, vaults, journal_entries, rate_limits)` (for 000003), `AUDIT 2025-02-23: add owner RLS policies and indexes for user_id tables`.

---

## Validation finale (post correctif GRANTs)

Après exécution de `docs/RLS_GRANTS_FIX.sql`, relancer `docs/RLS_SMOKE_TEST_READONLY.sql` et valider point par point. Une fois les sorties conformes, considérer le volet RLS **bon pour prod**.

### Checklist de conformité

| Check | Attendu | Statut |
|-------|---------|--------|
| RLS sur `public.user_zone_state` | NOTICE « RLS is enabled on public.user_zone_state » | ✅ |
| Policies sur `user_zone_state` | Compteur ≥ 4 (SELECT, INSERT, UPDATE, DELETE ou policy ALL owner) | ✅ |
| GRANTs **anon** | Aucun privilège sur `user_zone_state`, `arche_rate_limits`, `activation_codes` | ✅ |
| GRANTs **authenticated** | SELECT/INSERT/UPDATE/DELETE sur `user_zone_state` uniquement ; aucun sur `arche_rate_limits` / `activation_codes` | ✅ |
| GRANTs **service_role** | Droits complets (ou suffisants) sur les tables concernées | ✅ |
| Visibilité service-managed | `SELECT count(*)` sur `arche_rate_limits` et `activation_codes` en rôle authenticated → **0** (si SET ROLE refusé dans l’éditeur, exécuter les deux requêtes en client avec JWT) | ✅ |

### Critères de conformité (rappel)

- **RLS** activée sur `public.user_zone_state`.
- **Policies** : au moins 4 sur `user_zone_state`, couvrant au minimum SELECT, INSERT, UPDATE, DELETE (ou une policy ALL avec `auth.uid() = user_id`).
- **GRANTs** : anon sans accès aux tables owner et service-managed ; authenticated uniquement sur tables owner (user_zone_state, etc.) ; service_role avec droits nécessaires.
- **Service-managed** : aucune ligne visible pour un rôle `authenticated` sans JWT sur `arche_rate_limits` et `activation_codes`.

### Observations

_(Exemple si counts vérifiés en client JWT :)_  
_Counts service-managed vérifiés via client JWT (SET ROLE non autorisé dans l’éditeur). Résultat : 0/0._

_(Autres écarts éventuels : tables 000003 non déployées, etc.)_

### Statut

**Bon pour prod** — Isolation par `user_id` via policies RLS ; tables service-managed inaccessibles aux clients (anon/authenticated) ; correctif GRANTs appliqué et smoke test read-only conforme.

_(Dès que tu as exécuté `RLS_GRANTS_FIX.sql` et relancé le smoke test read-only, vérifie les points ci-dessus. Si tu partages les sorties ou un « OK conforme » point par point, le bloc « Validation finale » peut être complété avec d’éventuelles observations.)_
